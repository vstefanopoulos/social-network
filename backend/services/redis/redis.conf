# redis.conf - simple Docker config without persistence

# Bind to all interfaces
bind 0.0.0.0

# Default port
port 6379

# Disable protected mode for external connections
protected-mode no

# Disable saving snapshots
save ""

# Disable AOF (append-only file)
appendonly no
